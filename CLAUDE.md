@AGENTS.md

<!-- Claude Code auto-loads this file; every other coding agent reads AGENTS.md.
     The import above is the whole adapter, so the instructions have exactly one
     home. Claude-specific notes (nothing else) may go below. Delete this comment
     during setup — the `@AGENTS.md` line is the only part that must survive. -->
