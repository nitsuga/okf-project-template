# docs/ — human-facing guides

Terse, authored documentation for **human** readers: getting-started, API/usage
guides, tutorials. Not agent-maintained knowledge (that's `context/`) and not the
plan (that's `planning/`).

Keep it short and current; link to `context/` or `references/` for depth rather
than restating them here.

This README is a placeholder; keep it or delete it once real guides land.
